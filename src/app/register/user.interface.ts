// Define a interface UserInterface
export interface UserInterface {
  email: string; // Propriedade para armazenar o email do usuário (do tipo string)
  username: string; // Propriedade para armazenar o nome de usuário (do tipo string)
}
